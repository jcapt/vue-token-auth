const defaultLoginCredentials = ['email', 'password']
const defaultRegisterCredentials = ['email', 'password', 'password_confirmation']

const parseLoginCredentials = (credentials = defaultLoginCredentials) => {

}

const parseRegisterCredentials = (credentials = defaultRegisterCredentials) => {

}

export{
  parseLoginCredentials,
  parseRegisterCredentials
}